<views:MvxWpfView 
    xmlns:views="clr-namespace:MvvmCross.Platforms.Wpf.Views;assembly=MvvmCross.Platforms.Wpf"
    xmlns:mvx="clr-namespace:MvvmCross.Platforms.Wpf.Binding;assembly=MvvmCross.Platforms.Wpf"
    x:Class="NhlPlayers.WPF.Views.ListingPlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NhlPlayers.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="10">
            <TextBox Text="{Binding FilterFirstName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBox Text="{Binding FilterLastName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBox Text="{Binding FilterClub, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

            <ComboBox ItemsSource="{Binding PlayersProperty}" SelectedItem="{Binding SelectedProperty, Mode=TwoWay}" />
            <Button mvx:Bi.nd="Command ImportPlayerCommand" Margin="0 0 0 15">Import</Button>
            <DataGrid ItemsSource="{Binding Players}" AutoGenerateColumns="True"></DataGrid>
        </StackPanel>
    </Grid>
</views:MvxWpfView>
